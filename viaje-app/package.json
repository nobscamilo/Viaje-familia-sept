{
  "name": "viaje-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "node --test \"test/**/*.test.js\"",
    "check:size": "node scripts/check-file-size.mjs",
    "seed": "node scripts/seed.mjs",
    "seed:write": "node scripts/seed.mjs --write",
    "rules": "npm run probar:reglas && firebase deploy --only firestore:rules,firestore:indexes --project viaje-familia-sept-2026",
    "deploy": "npm run build && firebase deploy --only hosting,firestore:rules,firestore:indexes,functions --project viaje-familia-sept-2026",
    "deploy:web": "npm run build && firebase deploy --only hosting --project viaje-familia-sept-2026",
    "check:dangling": "node scripts/check-dangling.mjs",
    "check": "npm run check:size && npm run check:dangling && npm test",
    "publicar": "npm run check && node scripts/seed.mjs --write && npm run build && firebase deploy --only hosting --project viaje-familia-sept-2026",
    "probar:sitio": "node functions/probar-sitio.mjs",
    "codigos": "node scripts/codigos.mjs",
    "codigos:write": "node scripts/codigos.mjs --write",
    "medir": "node scripts/medir.mjs",
    "probar:reglas": "node scripts/probar-reglas.mjs",
    "probar:entrada": "node scripts/probar-entrada.mjs",
    "reparar:enlace": "node scripts/reparar-enlace.mjs",
    "probar:planes": "node scripts/probar-planes.mjs",
    "mover:codigos": "node scripts/mover-codigos.mjs"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-router": "^7.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "firebase-admin": "^13.9.0",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.2.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.5.0",
    "vite": "^8.0.10"
  }
}
