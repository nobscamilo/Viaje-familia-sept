*.docx
.DS_Store
node_modules/
dist/
.env
.env.local
.env.*.local
npm-debug.log*

# --- Secretos (anadido 2026-08-26) ---
# El repo es publico. Las claves de cuenta de servicio dan acceso de
# administrador y se saltan las reglas de Firestore: nunca aqui dentro.
.env
.env.local
.env.*.local
*serviceaccount*.json
*service-account*.json
*service_account*.json
*firebase-adminsdk*.json
*adminsdk*.json
*-key.json
credentials.json
diseno-import/

# --- Documentos privados de la familia (anadido 2026-09-06) ---
# Dosier de reservas: nombres completos de nueve personas (dos menores),
# direcciones y el numero de reserva del ibis Saint-Maurice, que ES el
# codigo del portal. El repo es publico: nada de esto se sube.
docs-privados/
